services:
  - type: web
    name: financias
    runtime: node
    buildCommand: npm run render-build
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
